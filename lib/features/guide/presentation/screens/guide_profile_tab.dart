import 'package:flutter/material.dart';

class GuideProfileTab extends StatelessWidget {
  const GuideProfileTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Guide Profile Tab - Coming Soon'),
    );
  }
} 