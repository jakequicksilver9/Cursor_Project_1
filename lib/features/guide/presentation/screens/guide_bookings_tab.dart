import 'package:flutter/material.dart';

class GuideBookingsTab extends StatelessWidget {
  const GuideBookingsTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Guide Bookings Tab - Coming Soon'),
    );
  }
} 